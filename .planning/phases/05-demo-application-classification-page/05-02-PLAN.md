---
phase: 05-demo-application-classification-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - app/src/__init__.py
  - app/src/main.py
  - app/src/config.py
  - app/src/routes/__init__.py
  - app/src/routes/classify.py
  - app/src/services/__init__.py
  - app/src/templates/base.html
  - app/src/static/css/app.css
  - app/src/static/samples/.gitkeep
  - app/tests/__init__.py
  - app/tests/conftest.py
autonomous: true

must_haves:
  truths:
    - "FastAPI app starts and serves a page at localhost:8000"
    - "App directory structure follows the research-specified layout"
  artifacts:
    - path: "app/src/main.py"
      provides: "FastAPI application factory with lifespan"
      contains: "async def lifespan"
    - path: "app/src/config.py"
      provides: "App configuration with model paths, labels, sample paths"
      contains: "RVL_CDIP_LABELS"
    - path: "app/src/routes/classify.py"
      provides: "GET / and POST /classify route stubs"
      contains: "router"
  key_links:
    - from: "app/src/main.py"
      to: "app/src/routes/classify.py"
      via: "router include"
      pattern: "app\\.include_router"
---

<objective>
Install web dependencies and create the FastAPI application scaffolding with directory structure, configuration, route stubs, and base template.

Purpose: Establishes the app skeleton that all subsequent plans build upon. The app must start and serve a placeholder page to validate the foundation before adding inference and visualization logic.

Output: Working FastAPI app at app/src/ with config, routes, templates, and static directories. App starts with `poetry run uvicorn app.src.main:app` and serves a page at localhost:8000.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-demo-application-classification-page/05-RESEARCH.md
@/tmp/stitch-demo.html
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install web dependencies and create app directory structure</name>
  <files>pyproject.toml, app/src/__init__.py, app/src/services/__init__.py, app/src/routes/__init__.py, app/tests/__init__.py, app/src/static/css/app.css, app/src/static/samples/.gitkeep</files>
  <action>
1. Add dependencies to pyproject.toml via `poetry add fastapi uvicorn jinja2 jinja2-fragments python-multipart`. If poetry add fails due to SSL issues (known project blocker), manually add them to pyproject.toml dependencies list instead.

2. Create the directory structure:
```
app/
  src/
    __init__.py           # empty
    routes/
      __init__.py         # empty
    services/
      __init__.py         # empty
    templates/            # created, populated in task 2
    static/
      css/
        app.css           # Tailwind config script + custom styles from Stitch prototype
      samples/
        .gitkeep          # placeholder for sample images
      js/                 # empty dir (HTMX/Alpine come from CDN)
  tests/
    __init__.py           # empty
```

3. Create app/src/static/css/app.css with the custom styles from the Stitch prototype:
- .material-symbols-outlined font-variation-settings
- body font-family Inter
- .font-headline Manrope
- .font-label Space Grotesk
- .custom-glass backdrop-filter blur

Note: Tailwind CSS and its config come from CDN via script tag in base.html template, NOT from app.css. The app.css only contains custom classes not handled by Tailwind.
  </action>
  <verify>
`ls app/src/routes/__init__.py app/src/services/__init__.py app/tests/__init__.py app/src/static/css/app.css app/src/static/samples/.gitkeep` — all files exist.
`poetry run python -c "import fastapi; import jinja2_fragments; print('deps OK')"` — imports succeed.
  </verify>
  <done>App directory structure exists. Web dependencies installed and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Create app config, main.py, route stubs, base template, and test conftest</name>
  <files>app/src/config.py, app/src/main.py, app/src/routes/classify.py, app/src/templates/base.html, app/tests/conftest.py</files>
  <action>
**app/src/config.py:**
- Define RVL_CDIP_LABELS list: ["letter", "form", "email", "handwritten", "advertisement", "scientific report", "scientific publication", "specification", "file folder", "news article", "budget", "invoice", "presentation", "questionnaire", "resume", "memo"]
- Define MODEL_CHECKPOINTS dict mapping display names to checkpoint paths (relative to project root):
  - "CNN Baseline (ResNet-50)": "models/exp14b_finetuned_resnet50_cnn.pt"
  - "Fusion GraphSAGE": "models/exp16_fusion_featknn_graphsage.pt"
  - "Fusion GAT": "models/exp23_gat_fusion.pt"
  - "BoC GraphSAGE": "models/exp25_boc_sage.pt"
  - "Gated BoC GraphSAGE": "models/exp26_gated_boc.pt"
  - "Attention Pool GraphSAGE": "models/exp27_attn_pool.pt"
- Define SAMPLES_DIR path and STATIC_DIR, TEMPLATES_DIR paths
- Define PROJECT_ROOT using Path(__file__).resolve().parent.parent.parent (app/src/config.py -> project root)

**app/src/main.py:**
- Create FastAPI app with lifespan context manager pattern
- In lifespan: placeholder comments for model loading (implemented in plan 03)
- Mount static files from app/src/static at /static
- Configure Jinja2Blocks templates from app/src/templates
- Include classify router
- Add CORS middleware (for localhost dev)

**app/src/routes/classify.py:**
- Create APIRouter
- GET "/" — renders base.html full page with empty results context
- POST "/classify" — stub that returns "Not implemented" (filled in plan 05)

**app/src/templates/base.html:**
- Convert the Stitch prototype (/tmp/stitch-demo.html) into a Jinja2 template
- Include Tailwind CDN script with the MD3 color tokens config from the prototype
- Include Google Fonts (Manrope, Inter, Space Grotesk, Material Symbols)
- Include HTMX 2.0 from CDN
- Include Alpine.js 3.x from CDN
- Link app.css from /static/css/app.css
- Structure the layout with Jinja2 blocks:
  - {% block upload_zone %} — left panel upload area + sample thumbnails
  - {% block results_section %} — right panel (initially shows "Upload a document to begin" placeholder)
- The results_section block will contain sub-blocks (added in plan 05):
  - {% block cnn_features %}
  - {% block graph_construction %}
  - {% block model_predictions %}
  - {% block detailed_analysis %}
- Keep all the MD3 design tokens, Tailwind classes, and styling from the Stitch prototype
- Use the exact HTML structure from /tmp/stitch-demo.html but with Jinja2 template variables where dynamic content goes
- The upload zone's form should POST to /classify with hx-post, hx-target="#results", hx-swap="innerHTML"

**app/tests/conftest.py:**
- Create TestClient fixture using FastAPI TestClient
- Import app from app.src.main
- Fixture provides test client that can be used by test functions
  </action>
  <verify>
Run: `cd /Users/sagarpratapsingh/dev/sagerstack/deep-learning-doc-classification && poetry run uvicorn app.src.main:app --host 0.0.0.0 --port 8000 &` then `sleep 2 && curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/` — must return 200. Then kill the server.
  </verify>
  <done>FastAPI app starts, serves the Stitch-derived base template at localhost:8000 with upload zone visible, and test conftest provides working TestClient fixture.</done>
</task>

</tasks>

<verification>
1. `poetry run uvicorn app.src.main:app` starts without errors
2. GET / returns 200 with HTML containing the Stitch-derived layout
3. POST /classify returns a response (stub is fine)
4. Static files served at /static/css/app.css
5. Test client fixture works: `poetry run python -c "from app.tests.conftest import *; print('conftest OK')"`
</verification>

<success_criteria>
FastAPI app serves the classification demo page skeleton at localhost:8000 with the Stitch-derived UI (header, left panel upload zone, right panel placeholder). The app structure is ready for services and template content to be added in subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/05-demo-application-classification-page/05-02-SUMMARY.md`
</output>
