---
phase: 02.1-hybrid-fusion-positional-encoding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features.py
  - src/graph.py
  - src/model.py
autonomous: true

must_haves:
  truths:
    - "Hybrid feature caching extracts both layer4 [2048,7,7] AND avgpool [2048] from a fine-tuned ResNet-50 in a single pass"
    - "Cached .pt files contain three keys: features, global_feat, and label"
    - "Graph construction appends normalized 2D positional encoding to node features, producing [49, 2050] node tensors"
    - "PyG Data objects carry global_feat as [1, 2048] tensor that survives batching to produce [batch_size, 2048]"
    - "HybridGraphSAGE model accepts both graph data and global_feat, producing logits [B, 16]"
  artifacts:
    - path: "src/features.py"
      provides: "Hybrid feature extraction caching both layer4 spatial and avgpool global features"
      exports: ["create_extractor_from_trained_hybrid", "cache_features_hybrid", "load_cached_features_hybrid"]
    - path: "src/graph.py"
      provides: "2D positional encoding injection and hybrid graph construction with global_feat"
      exports: ["add_positional_encoding_2d", "feature_map_to_graph_hybrid", "build_graph_dataset_hybrid"]
    - path: "src/model.py"
      provides: "HybridGraphSAGE model with CNN+GNN fusion classifier"
      exports: ["HybridGraphSAGE"]
  key_links:
    - from: "src/features.py"
      to: "torchvision.models.feature_extraction"
      via: "create_feature_extractor for both layer4 and avgpool return nodes"
      pattern: "create_feature_extractor.*layer4.*avgpool"
    - from: "src/graph.py"
      to: "src/features.py"
      via: "reads cached dicts with global_feat key from hybrid caching"
      pattern: 'item\["global_feat"\]'
    - from: "src/graph.py"
      to: "torch_geometric.data.Data"
      via: "stores global_feat as custom attribute with unsqueeze(0) for correct batching"
      pattern: "data\\.global_feat.*unsqueeze"
    - from: "src/model.py"
      to: "src/graph.py"
      via: "forward pass receives global_feat from PyG batched Data objects"
      pattern: "def forward.*global_feat"
---

<objective>
Add hybrid CNN+GNN fusion pipeline to the source modules: dual feature extraction (layer4 + avgpool), 2D positional encoding on graph nodes, and HybridGraphSAGE model with concatenation fusion.

Purpose: Enable the GraphSAGE model to leverage both local spatial structure (via GNN on 7x7 feature map graph) and global context (via ResNet-50 avgpool), with positional encoding giving the GNN spatial awareness. This is the core architectural enhancement for Phase 2.1.

Output: Updated src/features.py, src/graph.py, src/model.py with hybrid functions alongside existing functions (backward compatible).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-hybrid-fusion-positional-encoding/02.1-RESEARCH.md
@.planning/phases/01-notebook-foundation-data-features/01-01-SUMMARY.md
@src/config.py
@src/data.py
@src/features.py
@src/graph.py
@src/model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hybrid feature extraction and caching to features.py</name>
  <files>src/features.py</files>
  <action>
Add three new functions to src/features.py. Keep all existing functions untouched (backward compatibility).

**`create_extractor_from_trained_hybrid(model, config: Config)`**:
- Creates a SINGLE feature extractor that returns BOTH layer4 and avgpool from an already-trained ResNet-50.
- Use `create_feature_extractor(model, return_nodes={"layer4": "layer4", "avgpool": "avgpool"})` — one extractor, two return nodes. This is more efficient than two separate extractors (single forward pass).
- Set to `.eval()`, freeze all parameters (`param.requires_grad = False`), move to `config.device`.
- Print info: "Hybrid feature extractor loaded" with parameter count and "Output: layer4 [B, 2048, 7, 7] + avgpool [B, 2048, 1, 1]".
- Return the extractor.

**`cache_features_hybrid(samples, extractor, transform, config: Config, split: str)`**:
- Similar to existing `cache_features` but uses the hybrid extractor.
- Creates cache directory at `config.cache_dir / f"{split}_hybrid"` (separate from non-hybrid cache).
- Processes in batches of `config.batch_size`:
  - Apply transform and stack into tensor [B, 3, 224, 224], move to device.
  - Run extractor with `torch.no_grad()`:
    - `output = extractor(batch_images)`
    - `layer4_features = output["layer4"]` — shape [B, 2048, 7, 7]
    - `avgpool_features = output["avgpool"].squeeze(-1).squeeze(-1)` — shape [B, 2048] (squeeze the [1,1] spatial dims)
  - Save each sample individually as: `torch.save({"features": layer4_cpu[i], "global_feat": avgpool_cpu[i], "label": sample["label"]}, cache_path)`
  - Move tensors to CPU before saving (device-agnostic).
- Show progress with tqdm.
- Print total cached count and directory path.
- Return cache directory path.

**`load_cached_features_hybrid(config: Config, split: str)`**:
- Loads from `config.cache_dir / f"{split}_hybrid"`.
- Returns list of dicts with "features" [2048, 7, 7], "global_feat" [2048], and "label" int.
- Sort files by name to preserve order.
- Use `weights_only=False` (dict contains non-tensor int label — same pattern as existing `load_cached_features`).
- Print loaded count.

Important anti-patterns:
- Do NOT create two separate extractors (wasteful — one forward pass extracts both).
- Do NOT forget to squeeze avgpool output from [B, 2048, 1, 1] to [B, 2048].
- Do NOT store in same cache dir as existing features — use `{split}_hybrid` suffix to avoid conflicts.
  </action>
  <verify>
Run: `poetry run python -c "
import torch
from src.config import Config
from src.features import create_extractor_from_trained_hybrid, cache_features_hybrid, load_cached_features_hybrid
from torchvision.models import resnet50, ResNet50_Weights

c = Config()

# Use pretrained model as stand-in for fine-tuned model
model = resnet50(weights=ResNet50_Weights.IMAGENET1K_V2)
extractor = create_extractor_from_trained_hybrid(model, c)

# Test extraction with dummy data
dummy = torch.randn(2, 3, 224, 224).to(c.device)
with torch.no_grad():
    out = extractor(dummy)
print(f'layer4: {out[\"layer4\"].shape}')
print(f'avgpool: {out[\"avgpool\"].shape}')
assert out['layer4'].shape == (2, 2048, 7, 7), 'Wrong layer4 shape'
assert out['avgpool'].shape == (2, 2048, 1, 1), 'Wrong avgpool shape'

# Test caching with real data
from src.data import load_rvl_cdip, get_transform
data = load_rvl_cdip(c)
t = get_transform()
cache_features_hybrid(data['train'][:4], extractor, t, c, 'train_test')
loaded = load_cached_features_hybrid(c, 'train_test')
print(f'Cached: {len(loaded)} files')
print(f'features shape: {loaded[0][\"features\"].shape}')
print(f'global_feat shape: {loaded[0][\"global_feat\"].shape}')
print(f'label: {loaded[0][\"label\"]}')
assert loaded[0]['features'].shape == (2048, 7, 7)
assert loaded[0]['global_feat'].shape == (2048,)
print('All features.py checks passed')
"
`
Expected: layer4 [2, 2048, 7, 7], avgpool [2, 2048, 1, 1], cached 4 files with correct shapes.
  </verify>
  <done>Hybrid extractor produces both layer4 and avgpool from single forward pass, caching saves {features, global_feat, label} dicts, loading restores all three fields with correct shapes</done>
</task>

<task type="auto">
  <name>Task 2: Add positional encoding and hybrid graph construction to graph.py</name>
  <files>src/graph.py</files>
  <action>
Add three new functions to src/graph.py. Keep all existing functions untouched.

**`add_positional_encoding_2d(x: torch.Tensor, grid_h: int = 7, grid_w: int = 7) -> torch.Tensor`**:
- Appends normalized (row, col) coordinates to node features.
- Generate normalized coordinates in [0, 1]:
  - `rows = torch.arange(grid_h, dtype=torch.float32) / (grid_h - 1)` — values [0, 0.167, ..., 1.0]
  - `cols = torch.arange(grid_w, dtype=torch.float32) / (grid_w - 1)`
- Create grid: `row_coords = rows.repeat_interleave(grid_w)`, `col_coords = cols.repeat(grid_h)`
- Stack: `pos_enc = torch.stack([row_coords, col_coords], dim=1)` — shape [49, 2]
- Concatenate: `torch.cat([x, pos_enc.to(x.device)], dim=1)` — returns [num_nodes, feature_dim + 2]
- Important: use `pos_enc.to(x.device)` to handle any device mismatch.

**`feature_map_to_graph_hybrid(features: torch.Tensor, global_feat: torch.Tensor, label: int, edge_index: torch.Tensor) -> Data`**:
- Same as existing `feature_map_to_graph` but with PE and global_feat.
- Reshape features [C, H, W] to [H*W, C] node features (same as existing).
- Call `add_positional_encoding_2d(x, grid_h=h, grid_w=w)` to get [49, 2050].
- Create Data object with x, edge_index, y.
- CRITICAL: Store global_feat as 2D tensor: `data.global_feat = global_feat.unsqueeze(0)` — shape [1, 2048]. This ensures PyG batching concatenates along dim=0 to produce [batch_size, 2048] instead of flattening to [batch_size * 2048].
- Return the Data object.

**`build_graph_dataset_hybrid(cached_features: List[Dict], edge_index: torch.Tensor) -> List[Data]`**:
- Same pattern as existing `build_graph_dataset` but calls `feature_map_to_graph_hybrid`.
- Each dict must have "features", "global_feat", and "label" keys (from hybrid caching).
- Return list of PyG Data objects.

Important anti-patterns:
- Do NOT apply PE in the model forward pass — it must happen during graph construction (before Data creation).
- Do NOT store global_feat as 1D [2048] — PyG batching will flatten it. Must be [1, 2048].
- Do NOT use raw unnormalized indices [0,1,2,...,6] for PE — normalize to [0, 1] range.
  </action>
  <verify>
Run: `poetry run python -c "
import torch
from src.graph import add_positional_encoding_2d, feature_map_to_graph_hybrid, build_graph_dataset_hybrid, build_grid_edge_index

# Test PE
x = torch.randn(49, 2048)
x_pe = add_positional_encoding_2d(x, 7, 7)
print(f'PE input: {x.shape} -> output: {x_pe.shape}')
assert x_pe.shape == (49, 2050), 'Wrong PE shape'
assert x_pe[0, -2] == 0.0, 'First row coord should be 0'
assert x_pe[0, -1] == 0.0, 'First col coord should be 0'
assert abs(x_pe[-1, -2] - 1.0) < 1e-6, 'Last row coord should be 1'
assert abs(x_pe[-1, -1] - 1.0) < 1e-6, 'Last col coord should be 1'

# Test hybrid graph construction
edge_index = build_grid_edge_index(7, 7, k=8)
features = torch.randn(2048, 7, 7)
global_feat = torch.randn(2048)
data = feature_map_to_graph_hybrid(features, global_feat, label=3, edge_index=edge_index)
print(f'x: {data.x.shape}, y: {data.y}, global_feat: {data.global_feat.shape}')
assert data.x.shape == (49, 2050), 'Wrong node feature shape'
assert data.global_feat.shape == (1, 2048), 'global_feat must be 2D [1, 2048]'
assert data.y.item() == 3

# Test batch building
cached = [{'features': torch.randn(2048, 7, 7), 'global_feat': torch.randn(2048), 'label': i} for i in range(4)]
graphs = build_graph_dataset_hybrid(cached, edge_index)
print(f'Built {len(graphs)} graphs')
assert len(graphs) == 4

# Test PyG batching preserves global_feat shape
from torch_geometric.loader import DataLoader
loader = DataLoader(graphs, batch_size=2)
batch = next(iter(loader))
print(f'Batched global_feat: {batch.global_feat.shape}')
assert batch.global_feat.shape == (2, 2048), 'Batching must produce [batch_size, 2048]'
print('All graph.py checks passed')
"
`
Expected: PE [49, 2050], correct corner coordinates, Data with [49, 2050] nodes, batched global_feat [2, 2048].
  </verify>
  <done>Positional encoding appends normalized 2D coords to node features [2048]->[2050], hybrid graph construction stores global_feat as [1, 2048] surviving PyG batching to [batch_size, 2048]</done>
</task>

<task type="auto">
  <name>Task 3: Add HybridGraphSAGE model to model.py</name>
  <files>src/model.py</files>
  <action>
Add the `HybridGraphSAGE` class to src/model.py. Keep existing `GraphSAGEClassifier` untouched.

**`HybridGraphSAGE(nn.Module)`**:
- Docstring: "Two-layer GraphSAGE with CNN+GNN hybrid fusion classifier."
- Constructor args:
  - `node_dim: int = 2050` (2048 features + 2 PE coords)
  - `hidden_channels: int = 256`
  - `embed_channels: int = 128`
  - `global_channels: int = 2048` (ResNet-50 avgpool dimension)
  - `num_classes: int = NUM_CLASSES` (use existing env-driven NUM_CLASSES)
  - `dropout: float = 0.5`
- Layers:
  - `self.conv1 = SAGEConv(node_dim, hidden_channels)` — node_dim=2050 not 2048
  - `self.conv2 = SAGEConv(hidden_channels, embed_channels)`
  - `self.dropout = nn.Dropout(dropout)`
  - `self.relu = nn.ReLU()`
  - `fusion_dim = embed_channels + global_channels` — 128 + 2048 = 2176
  - `self.classifier = nn.Linear(fusion_dim, num_classes)`

- Forward method signature: `forward(self, x, edge_index, batch, global_feat)`:
  - GNN path: conv1 -> relu -> dropout -> conv2 -> relu -> global_mean_pool(x, batch) -> gnn_embed [batch_size, 128]
  - Fusion: `fused = torch.cat([gnn_embed, global_feat], dim=1)` — [batch_size, 2176]
    NOTE: global_feat arrives as [batch_size, 2048] from PyG batching (already correct shape from the [1, 2048] per-graph storage). No squeeze needed if PyG produces [batch_size, 2048] directly.
    BUT if PyG batching produces [batch_size, 1, 2048] (since per-graph is [1, 2048] and cat_dim=0 adds a batch dim), handle with: `if global_feat.dim() == 3: global_feat = global_feat.squeeze(1)`.
    VERIFY during testing which shape PyG actually produces. The graph.py verify script above tests this — use the same shape handling.
  - Classification: `logits = self.classifier(fused)` — returns [batch_size, num_classes]

- Add `import torch.nn.functional as F` at top of file if not already present (for consistency, though using self.relu is fine too).

Important anti-patterns:
- Do NOT add a projection layer before fusion (e.g., Linear(2048, 256) for CNN features) — unnecessary parameters on small dataset per research recommendation.
- Do NOT change the existing GraphSAGEClassifier class — it must remain for comparison.
- Do NOT hardcode num_classes=16 — use the existing NUM_CLASSES env pattern.
  </action>
  <verify>
Run: `poetry run python -c "
import torch
from src.model import HybridGraphSAGE, GraphSAGEClassifier

# Verify existing model still works
old_model = GraphSAGEClassifier(in_channels=2048)
print(f'Old model params: {sum(p.numel() for p in old_model.parameters()):,}')

# Test HybridGraphSAGE
model = HybridGraphSAGE(node_dim=2050, hidden_channels=256, embed_channels=128, global_channels=2048, num_classes=16, dropout=0.5)
print(f'Hybrid model params: {sum(p.numel() for p in model.parameters()):,}')

# Forward pass with dummy data
num_nodes = 49 * 4  # 4 graphs of 49 nodes
x = torch.randn(num_nodes, 2050)
edge_index = torch.randint(0, num_nodes, (2, num_nodes * 8))
batch = torch.arange(4).repeat_interleave(49)
global_feat = torch.randn(4, 2048)

model.eval()
with torch.no_grad():
    logits = model(x, edge_index, batch, global_feat)
print(f'Output logits: {logits.shape}')
assert logits.shape == (4, 16), f'Expected (4, 16), got {logits.shape}'
print('All model.py checks passed')
"
`
Expected: Old model params printed, hybrid model params printed, logits shape [4, 16].
  </verify>
  <done>HybridGraphSAGE produces [batch_size, 16] logits from graph data + global CNN features via concatenation fusion, existing GraphSAGEClassifier remains untouched</done>
</task>

</tasks>

<verification>
1. `poetry run python -c "from src.features import create_extractor_from_trained_hybrid, cache_features_hybrid, load_cached_features_hybrid"` — hybrid feature imports work
2. `poetry run python -c "from src.graph import add_positional_encoding_2d, feature_map_to_graph_hybrid, build_graph_dataset_hybrid"` — hybrid graph imports work
3. `poetry run python -c "from src.model import HybridGraphSAGE, GraphSAGEClassifier"` — both models importable
4. Hybrid extractor extracts both layer4 [B, 2048, 7, 7] and avgpool [B, 2048] in single forward pass
5. PE produces correct normalized coordinates at corners: (0,0) at top-left, (1,1) at bottom-right
6. PyG batching of global_feat produces [batch_size, 2048] not flattened
7. HybridGraphSAGE forward pass produces [batch_size, num_classes] logits
8. Existing functions (create_resnet_extractor, feature_map_to_graph, GraphSAGEClassifier) still work unchanged
</verification>

<success_criteria>
- Hybrid extractor uses single create_feature_extractor with two return nodes (not two extractors)
- Cached hybrid .pt files contain {features: [2048,7,7], global_feat: [2048], label: int}
- PE appends normalized [0,1] coordinates, producing [49, 2050] node features
- global_feat stored as [1, 2048] per Data object, batches to [batch_size, 2048]
- HybridGraphSAGE fusion: concat [gnn_embed:128 + cnn_global:2048] -> Linear(2176, 16)
- All existing functions remain backward compatible
- All verification scripts pass
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-hybrid-fusion-positional-encoding/02.1-01-SUMMARY.md`
</output>
