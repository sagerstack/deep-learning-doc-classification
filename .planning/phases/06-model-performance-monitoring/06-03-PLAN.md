---
phase: 06-model-performance-monitoring
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
gap_closure: true
autonomous: true
files_modified:
  - app/src/routes/classify.py
  - app/src/routes/monitoring.py
  - app/src/templates/base.html
  - app/src/templates/partials/model_predictions.html
  - app/tests/test_monitoring.py
  - app/tests/test_routes.py

must_haves:
  truths:
    - "The top navigation bar contains a 'Drift Monitoring' item that routes to /model-performance"
    - "The duplicate 'Model Performance' anchor in the left sidebar is removed"
    - "Each row in the Model Predictions table renders a thumbs-up and a thumbs-down button in the 'Correct?' column"
    - "Clicking thumbs-up sends an HTMX POST to /label with request_id, model_id, correct=true; the server writes target = predicted_label for that (request_id, model_id) row in inference_events"
    - "Clicking thumbs-down sends an HTMX POST to /label with request_id, model_id, correct=false; the server leaves target NULL (no overwrite)"
    - "After a label POST, the row's UI swaps to show a confirmation state (checkmark or X) so the user knows the click was recorded"
    - "The inference_events.target column is pre-declared in the CREATE TABLE DDL so operators can backfill via UPDATE without running ALTER TABLE first"
  artifacts:
    - path: "app/src/routes/classify.py"
      provides: "NAV_ITEMS repointed; request_id threaded into result template context"
      contains: "/model-performance"
    - path: "app/src/routes/monitoring.py"
      provides: "POST /label route that updates inference_events.target"
      exports: ["router"]
    - path: "app/src/templates/base.html"
      provides: "Sidebar /model-performance anchor removed"
    - path: "app/src/templates/partials/model_predictions.html"
      provides: "Thumbs-up/down HTMX buttons per row, scoped by request_id + model_id"
      contains: "hx-post=\"/label\""
    - path: "app/src/monitoring/store.py"
      provides: "target TEXT column pre-declared in _CREATE_TABLE_SQL; idempotent ALTER TABLE migration in init_db"
      contains: "target"
  key_links:
    - from: "app/src/templates/partials/model_predictions.html"
      to: "app/src/routes/monitoring.py"
      via: "HTMX POST /label with request_id, model_id, correct"
      pattern: "hx-post=\"/label\""
    - from: "app/src/routes/monitoring.py"
      to: "app/src/monitoring/store.py"
      via: "UPDATE inference_events SET target = ? WHERE request_id = ? AND model_id = ?"
      pattern: "UPDATE inference_events"
    - from: "app/src/routes/classify.py"
      to: "app/src/templates/partials/model_predictions.html"
      via: "request_id passed through _build_result_context into template context"
      pattern: "\"request_id\":"
---

<objective>
Close the three open gaps from 06-UAT.md and 06-VERIFICATION.md:

- **Gap 1 (UAT):** The user rejected the sidebar "Model Performance" link and asked for the existing top-nav "Drift Monitoring" item to route to the monitoring page.
- **Gap 2 (UAT):** The "Correct?" column in the Model Predictions table is a dead placeholder. It must capture thumbs-up/down feedback and write the target label to inference_events.
- **Gap 3 (VERIFICATION):** Confirm `target TEXT` is pre-declared in the SQLite DDL so operators can backfill labels without ALTER TABLE.

Purpose: Must-haves 2 and 5 from Phase 6 are currently blocked. Closing them delivers the "Drift Monitoring" UX the user asked for and unlocks labeled monitoring end-to-end (UI → route → DB → Evidently ClassificationPreset).

Output: Repointed top-nav, removed redundant sidebar anchor, working thumbs-up/down feedback capture, verified target column DDL, and updated tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-model-performance-monitoring/06-01-SUMMARY.md
@.planning/phases/06-model-performance-monitoring/06-02-SUMMARY.md
@.planning/phases/06-model-performance-monitoring/06-UAT.md
@.planning/phases/06-model-performance-monitoring/06-VERIFICATION.md

@app/src/routes/classify.py
@app/src/routes/monitoring.py
@app/src/templates/base.html
@app/src/templates/partials/model_predictions.html
@app/src/monitoring/store.py
@app/src/monitoring/schema.py
@app/tests/test_monitoring.py
@app/tests/test_routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint top-nav and remove sidebar anchor (Gap 1)</name>
  <files>
    app/src/routes/classify.py
    app/src/templates/base.html
    app/tests/test_routes.py
  </files>
  <action>
**In `app/src/routes/classify.py`:**
- Update NAV_ITEMS (currently lines 35-41). Change the "drift-monitoring" entry so its `href` becomes `/model-performance` (keep label "Drift Monitoring" and key "drift-monitoring"). Final entry shape:
  `{"key": "drift-monitoring", "label": "Drift Monitoring", "href": "/model-performance"}`
- Do NOT add a new nav item; the existing one is being repointed per user feedback ("Drift Monitoring should route to this new page").

**In `app/src/templates/base.html`:**
- Delete the sidebar anchor block currently at lines 210-215 (the `<a href="/model-performance">...</a>` inside the `mt-auto pt-6 border-t ...` div). Preserve the surrounding `<div class="mt-auto pt-6 ...">` wrapper and the "RVL-CDIP Dataset" footer block below it. Only the Model Performance anchor is removed.
- If the wrapping `<div class="mt-auto pt-6 border-t border-slate-200/20 space-y-4">` becomes visually empty after removal, keep the div as-is (the dataset footer inside remains).

**In `app/tests/test_routes.py`:**
- Line 61 currently asserts `"/model-performance" in response.text` — this will still pass via the top-nav link, but update the test comment/intent to reflect the new source. If the test targets sidebar specifically, relax it to just check presence of `/model-performance` anywhere in the rendered base template.
- Add a new assertion (or a new test) verifying the top nav contains BOTH the label "Drift Monitoring" AND the href `/model-performance` in proximity (e.g. a single substring `href="/model-performance"` followed by `Drift Monitoring` within ~200 chars, or parse with a simple regex).
- Explicitly assert the sidebar no longer contains an anchor to `/model-performance` OUTSIDE the nav element. Simplest reliable approach: assert the `<aside>` block (the left sidebar) in the rendered HTML does NOT contain `href="/model-performance"`. Slice `response.text` from `<aside` to the matching `</aside>` and check.

Do not introduce a new route. Do not touch `/drift-monitoring` (no such route exists; it was a dead href).
  </action>
  <verify>
- `poetry run pytest app/tests/test_routes.py -v` passes.
- `poetry run python -c "from app.src.routes.classify import NAV_ITEMS; print([i for i in NAV_ITEMS if i['key']=='drift-monitoring'][0])"` prints an item whose `href` is `/model-performance`.
- Manual check: `poetry run uvicorn app.src.main:app --port 8000` then open `http://localhost:8000/` — top nav shows "Drift Monitoring" linking to `/model-performance`; left sidebar no longer shows a "Model Performance" entry.
  </verify>
  <done>Top-nav "Drift Monitoring" item routes to /model-performance; sidebar anchor is gone; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire thumbs-up/down feedback to POST /label → target column (Gap 2)</name>
  <files>
    app/src/routes/classify.py
    app/src/routes/monitoring.py
    app/src/templates/partials/model_predictions.html
    app/tests/test_monitoring.py
  </files>
  <action>
**In `app/src/routes/classify.py`:**
- `_build_result_context` already generates `request_id = str(uuid.uuid4())` inside the monitoring try/except (line 117). Lift `request_id` to be generated BEFORE the try/except (so it is available even if monitoring logging fails). Keep the try/except semantics for `log_inference_events` only.
- Add `"request_id": request_id` to the returned context dict (around lines 201-225). This is the single source of truth the template will use for feedback POSTs.

**In `app/src/templates/partials/model_predictions.html`:**
- Replace the dead "Correct?" cell (lines 35-41) with two HTMX-powered buttons per row:
  - Thumbs-up button: `hx-post="/label"`, `hx-vals='{"request_id": "{{ request_id }}", "model_id": "{{ r.model_name }}", "correct": true}'`, `hx-target="closest td"`, `hx-swap="innerHTML"`. Icon: `<span class="material-symbols-outlined">thumb_up</span>`. Tailwind: small icon button, green hover state, matching the MD3 surface-container tokens used elsewhere in the file.
  - Thumbs-down button: same structure with `"correct": false`. Icon: `thumb_down`. Red hover state.
- Wrap both in a single `<div class="flex gap-2">` cell so the swap target (the `<td>`) replaces both on click.
- The server will return an HTML fragment that confirms the feedback (see monitoring.py below). Tailwind classes should match the existing design system (font-label, text-[10px], rounded, etc.).
- Ensure the buttons use `r.model_name` (the model_id stored in inference_events) — NOT `r.display_name`. The routes file confirms `model_id=r.model_name` in build_inference_events.

**In `app/src/routes/monitoring.py`:**
- Add a new route: `@router.post("/label")` named `submit_label`. Accept `request_id: str`, `model_id: str`, `correct: bool` as form fields (FastAPI `Form(...)`).
- Validate: request_id non-empty, model_id non-empty. On invalid input return HTMLResponse with a small error fragment and status 400.
- On `correct=true`: open a sqlite3 connection to `MONITORING_DB_PATH` (import from `app.src.config`) and execute:
  `UPDATE inference_events SET target = predicted_label WHERE request_id = ? AND model_id = ?`
  Bind (request_id, model_id). If no rows updated, log a warning but still return a 200 with a confirmation fragment (the row may have been pruned).
- On `correct=false`: no DB write. Just acknowledge in the response.
- Return an HTMLResponse with a small confirmation fragment that replaces the buttons cell. Examples:
  - correct=true → `<span class="inline-flex items-center gap-1 text-green-600 font-label text-[10px] uppercase tracking-wider"><span class="material-symbols-outlined text-sm">check_circle</span>Recorded</span>`
  - correct=false → `<span class="inline-flex items-center gap-1 text-slate-500 font-label text-[10px] uppercase tracking-wider"><span class="material-symbols-outlined text-sm">cancel</span>Noted</span>`
- Wrap the DB write in try/except sqlite3.Error and log warning (same non-fatal posture as classify.py monitoring logging). On DB error return the confirmation fragment anyway (client UX should not block on DB).
- Do NOT introduce an ORM. Use stdlib `sqlite3` to match `store.py` conventions.

**In `app/tests/test_monitoring.py`:**
- Add a new test class or test block `TestLabelRoute` with three tests:
  1. `test_label_thumbs_up_writes_target`: seed one row into inference_events via `log_inference_events` with a known request_id/model_id/predicted_label. POST to `/label` with `correct=true`. Query the row back and assert `target == predicted_label`. Also assert response is 200 HTML containing "Recorded".
  2. `test_label_thumbs_down_leaves_target_null`: seed row, POST with `correct=false`. Assert `target IS NULL`. Assert response contains "Noted".
  3. `test_label_unknown_request_id_returns_ok`: POST with a random UUID and model_id not in DB. Assert status 200 (non-fatal). Assert no rows modified.
- Use the same temporary DB fixture pattern already in `test_monitoring.py` (see existing tests that use `MONITORING_DB_PATH` override).

Do NOT pass the raw FastAPI `Form(...)` through JSON — HTMX `hx-vals` posts form-encoded by default, which matches `Form(...)` parsing. Verify by inspecting HTMX docs if uncertain (no library change needed).
  </action>
  <verify>
- `poetry run pytest app/tests/test_monitoring.py -v` passes, including the three new label tests.
- `poetry run pytest app/tests/ -v` full suite stays green.
- Manual: run the app, classify a sample, click thumbs-up on the top result, then query SQLite:
  `poetry run python -c "from app.src.monitoring.store import query_events; from pathlib import Path; rows = query_events(Path('monitoring/data/inference_events.sqlite3'), limit=5); [print(r['request_id'][:8], r['model_id'], 'target=', r.get('target')) for r in rows]"`
  The most recent row for that model should show `target == predicted_label`.
- Manual: click thumbs-down, confirm the cell swaps to "Noted" and a follow-up query shows `target IS NULL`.
  </verify>
  <done>Thumbs-up writes target=predicted_label; thumbs-down leaves target NULL; cell visually confirms the click; tests cover all three cases and pass.</done>
</task>

<task type="auto">
  <name>Task 3: Verify target column DDL + migration (Gap 3)</name>
  <files>
    app/src/monitoring/store.py
    app/tests/test_monitoring.py
  </files>
  <action>
**Context:** Gap 3 from 06-VERIFICATION.md claimed `target TEXT` was missing from `_CREATE_TABLE_SQL`. Inspection of the current file shows this has already been addressed (commit a18de29): line 48 includes `target TEXT` in the DDL, and `init_db()` lines 77-80 run an idempotent `ALTER TABLE` via `PRAGMA table_info`. This task is to VERIFY the fix is in place and add a regression test so it cannot silently regress.

**In `app/src/monitoring/store.py`:**
- Re-read the file and confirm (no code changes expected):
  1. `_CREATE_TABLE_SQL` declares `target TEXT` (currently at line 48, just after `error_type TEXT,`).
  2. `init_db()` performs the `PRAGMA table_info(inference_events)` check and runs `ALTER TABLE inference_events ADD COLUMN target TEXT` when absent (currently lines 77-80).
  3. `_column_list()` and `_event_to_row()` both include `target` (currently lines 201 and 218).
- If any of the above is NOT actually present (e.g. was reverted), restore it per the gap.missing items in 06-VERIFICATION.md frontmatter. Otherwise proceed to the test below without code changes.

**In `app/tests/test_monitoring.py`:**
- Add a regression test `test_target_column_pre_declared_on_fresh_db`:
  1. Create a brand-new temp SQLite file path (do NOT pre-create the file).
  2. Call `init_db(path)`.
  3. Open a sqlite3 connection and run `PRAGMA table_info(inference_events)`.
  4. Assert `target` is in the returned column names.
  5. Assert the column's declared type is `TEXT` and its `notnull` flag is 0 (nullable).
- Add a second regression test `test_init_db_migrates_legacy_db_without_target`:
  1. Manually create a legacy-schema DB by executing a CREATE TABLE that intentionally omits the `target` column (copy `_CREATE_TABLE_SQL` into the test and strip the `target TEXT` line).
  2. Call `init_db(path)`.
  3. Assert `target` is now present via `PRAGMA table_info`.
  4. Insert one labeled row via raw SQL `UPDATE inference_events SET target = 'letter' WHERE request_id = ?` and assert no `OperationalError: no such column: target` is raised.

These two tests lock in the fix and make any future DDL regression visible in CI.
  </action>
  <verify>
- `poetry run pytest app/tests/test_monitoring.py::test_target_column_pre_declared_on_fresh_db -v` passes.
- `poetry run pytest app/tests/test_monitoring.py::test_init_db_migrates_legacy_db_without_target -v` passes.
- `poetry run python -c "import sqlite3, tempfile, pathlib; from app.src.monitoring.store import init_db; p = pathlib.Path(tempfile.mktemp(suffix='.sqlite3')); init_db(p); c = sqlite3.connect(p); print([r[1] for r in c.execute('PRAGMA table_info(inference_events)')])"` includes `'target'` in the printed list.
  </verify>
  <done>Fresh DB creation and legacy DB migration both yield a schema with `target TEXT`; regression tests enforce this going forward.</done>
</task>

</tasks>

<verification>
All three gaps from 06-UAT.md and 06-VERIFICATION.md are closed:

1. Top-nav "Drift Monitoring" routes to `/model-performance`; sidebar duplicate anchor removed.
2. Thumbs-up/down buttons in Model Predictions table POST to `/label` and update `inference_events.target` correctly.
3. `target TEXT` column is present in fresh DBs (DDL) and added to legacy DBs (migration); regression tests enforce both.

Run full suite: `poetry run pytest app/tests/ -v`. All green.

Smoke test: classify a sample → click thumbs-up on one row → query `target` column → confirm it matches `predicted_label` for that (request_id, model_id).
</verification>

<success_criteria>
- All 7 must-have truths in frontmatter are observable.
- `poetry run pytest app/tests/` passes with the new tests included.
- Top-nav "Drift Monitoring" item visibly links to `/model-performance` in the rendered HTML.
- Left sidebar contains no `/model-performance` anchor.
- POST `/label` with `correct=true` writes `target=predicted_label`; with `correct=false` writes nothing.
- PRAGMA table_info(inference_events) on a fresh DB includes a `target TEXT` column.
- `poetry run python scripts/monitoring/run_evidently.py --window-hours 24 --offline` still runs (sanity: the batch job already handles `target` presence conditionally; this change must not break it).
</success_criteria>

<output>
After completion, create `.planning/phases/06-model-performance-monitoring/06-03-SUMMARY.md` summarising:
- Which gaps were closed (1, 2, 3)
- Exact code locations changed
- New test names and what they cover
- Any follow-up concerns (e.g. if request_id persistence across HTMX re-renders needs future hardening)
</output>
