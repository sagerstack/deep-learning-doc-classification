---
phase: 02.2-text-aware-hybrid-gnn
plan: 02
type: execute
wave: 2
depends_on: ["02.2-01"]
files_modified:
  - notebooks/rvl-cdip-resnet50-baseline.ipynb
  - notebooks/rvl-cdip-text-aware-hybrid.ipynb
autonomous: false

must_haves:
  truths:
    - "Baseline notebook has an additive cell that augments existing cache with text_density [7,7] per sample"
    - "Text-aware hybrid notebook trains TextAwareGraphSAGE with node_dim=2051 end-to-end"
    - "Comparison section shows text-aware hybrid vs plain hybrid vs CNN baseline with per-class F1 breakdown"
  artifacts:
    - path: "notebooks/rvl-cdip-resnet50-baseline.ipynb"
      provides: "New Section 7.1 cell for text density cache augmentation"
      contains: "augment_cache_with_text_density"
    - path: "notebooks/rvl-cdip-text-aware-hybrid.ipynb"
      provides: "Complete text-aware hybrid training and evaluation notebook"
      contains: "TextAwareGraphSAGE"
  key_links:
    - from: "notebooks/rvl-cdip-resnet50-baseline.ipynb"
      to: "src/text_features.py"
      via: "imports augment_cache_with_text_density, create_text_detector"
      pattern: "from src.text_features import"
    - from: "notebooks/rvl-cdip-text-aware-hybrid.ipynb"
      to: "src/graph.py"
      via: "imports build_graph_dataset_text_aware"
      pattern: "build_graph_dataset_text_aware"
    - from: "notebooks/rvl-cdip-text-aware-hybrid.ipynb"
      to: "src/model.py"
      via: "imports TextAwareGraphSAGE"
      pattern: "TextAwareGraphSAGE"
    - from: "notebooks/rvl-cdip-text-aware-hybrid.ipynb"
      to: "checkpoints/hybrid_graphsage_metrics.json"
      via: "Section 7 three-way comparison loads Phase 2.1 hybrid metrics"
      pattern: "hybrid_graphsage_metrics.json"
    - from: "notebooks/rvl-cdip-text-aware-hybrid.ipynb"
      to: "checkpoints/resnet50_baseline_metrics.json"
      via: "Section 7 three-way comparison loads CNN baseline metrics"
      pattern: "resnet50_baseline_metrics.json"
---

<objective>
Create the baseline notebook cache augmentation cell and the complete text-aware hybrid notebook.

Purpose: Enable end-to-end text-aware training pipeline: baseline notebook augments cache with text density, then the new notebook loads augmented cache, builds text-aware graphs, trains TextAwareGraphSAGE, and compares against plain hybrid and CNN baseline.

Output: One modified notebook (baseline) and one new notebook (text-aware hybrid).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-text-aware-hybrid-gnn/02.2-CONTEXT.md
@.planning/phases/02.2-text-aware-hybrid-gnn/02.2-01-SUMMARY.md
@.planning/phases/02.1-hybrid-fusion-positional-encoding/02.1-02-SUMMARY.md
@src/text_features.py
@src/graph.py
@src/model.py
@src/features.py
@src/config.py
@notebooks/rvl-cdip-resnet50-baseline.ipynb
@notebooks/rvl-cdip-hybrid-graphsage.ipynb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add text density cache augmentation cell to baseline notebook and create text-aware hybrid notebook</name>
  <files>notebooks/rvl-cdip-resnet50-baseline.ipynb, notebooks/rvl-cdip-text-aware-hybrid.ipynb</files>
  <action>
**NOTE: This plan explicitly authorizes editing the baseline notebook and creating a new notebook.**

**Part A: Baseline notebook — add Section 7.1 cell**

Add a new markdown cell + code cell AFTER the existing Section 7 "Feature Extraction and Caching" (after cell-19 code), BEFORE Section 8 "Save Baseline Metrics".

Markdown cell:
```
## 7.1 Text Density Cache Augmentation

Augment cached features with text density from doctr DBNet detector. This adds a `text_density` [7,7] tensor to each cached .pt file without re-running ResNet-50 feature extraction. Existing keys (features, global_feat, label) are preserved.

**Prerequisites:** Run cells C1-C4 (setup, data loading) and Section 7 (feature caching) first.
**Skips:** Files that already have `text_density` key.
```

Code cell:
```python
# C20: Text density cache augmentation
from src.text_features import create_text_detector, augment_cache_with_text_density

text_detector = create_text_detector(config)

# Augment each split's cache with text density
for split_name, split_samples in [("train", train_samples), ("validation", val_samples), ("test", test_samples)]:
    print(f"\nAugmenting {split_name} cache with text density...")
    augment_cache_with_text_density(
        config=config,
        split=split_name,
        detector=text_detector,
        samples=split_samples,  # list of dicts with "image" key (PIL Image)
    )

# Verify: load one file and check text_density key exists
import torch
sample_path = list((config.cache_dir / "train").glob("*.pt"))[0]
sample = torch.load(sample_path, weights_only=False)
print(f"\nVerification - cached keys: {list(sample.keys())}")
print(f"text_density shape: {sample['text_density'].shape}")
```

The cell assumes `config`, `train_samples`, `val_samples`, `test_samples` exist from prior cells (same as how Section 7 uses them). It does NOT re-run feature extraction or model training.

The `augment_cache_with_text_density` call uses `samples=` keyword matching the definitive signature from Plan 01: `augment_cache_with_text_density(config, split, detector, samples)` where samples is a list of dicts with `"image"` PIL key.

**Part B: Create `notebooks/rvl-cdip-text-aware-hybrid.ipynb`**

Model this notebook closely on `notebooks/rvl-cdip-hybrid-graphsage.ipynb` (the Phase 2.1 hybrid notebook), with these changes:

Structure (similar 7-section layout, ~25 cells):

**Section 1: Setup and Imports** (2 cells: markdown + code)
- Same as hybrid notebook but import `build_graph_dataset_text_aware` instead of `build_graph_dataset_hybrid`
- Import `TextAwareGraphSAGE` instead of `HybridGraphSAGE`
- Import `load_cached_features` from src.features
- Same sys.path.insert(0, "..") pattern
- Same Config initialization and seed_everything

**Section 2: Load Cached Features** (2 cells: markdown + code)
- Same as hybrid notebook: load train/val/test from config.cache_dir
- Add verification that `text_density` key exists: `assert "text_density" in train_data[0], "Run baseline notebook Section 7.1 first to add text_density to cache"`

**Section 3: Graph Construction with Text Density** (3 cells: markdown + 2 code)
- Build edge_index with k=8
- Build graphs using `build_graph_dataset_text_aware` (NOT build_graph_dataset_hybrid)
- Verification cell: print graph stats, confirm node dim is 2051, show text_density range

**Section 4: DataLoader Setup** (2 cells: markdown + code)
- Same as hybrid: PyG DataLoader with batch_size, shuffle for train
- 80/20 train/val split if using sample mode

**Section 5: Model, Optimizer, Training** (4 cells: markdown + 3 code)
- Model instantiation: `TextAwareGraphSAGE(node_dim=2051, hidden_channels=64, embed_channels=32, dropout=0.7)`
- Same hyperparameters as Phase 2.1: lr=5e-4, label_smoothing=0.1, patience=15, min_epochs=20
- Training loop: same as hybrid notebook — forward pass calls `model(batch.x, batch.edge_index, batch.batch, batch.global_feat)`
- Early stopping with patience, save best model checkpoint

**Section 6: Evaluation** (3 cells: markdown + 2 code)
- Load best checkpoint, evaluate on test set
- Per-class F1, macro F1, accuracy, confusion matrix
- Save metrics JSON to `checkpoints/text_aware_hybrid_metrics.json`

**Section 7: Three-Way Comparison** (4 cells: markdown + 3 code)
- **Prerequisite check cell first:** Before loading metrics, verify both upstream files exist:
  ```python
  from pathlib import Path
  assert Path("checkpoints/resnet50_baseline_metrics.json").exists(), \
      "Missing checkpoints/resnet50_baseline_metrics.json - run baseline notebook Section 8 first"
  assert Path("checkpoints/hybrid_graphsage_metrics.json").exists(), \
      "Missing checkpoints/hybrid_graphsage_metrics.json - run hybrid notebook Section 6 first"
  ```
- Load metrics from:
  - `checkpoints/resnet50_baseline_metrics.json` (CNN baseline from baseline notebook)
  - `checkpoints/hybrid_graphsage_metrics.json` (Phase 2.1 hybrid)
  - `checkpoints/text_aware_hybrid_metrics.json` (this notebook)
- Comparison table: accuracy, macro F1 for all three models
- Per-class F1 breakdown table with delta columns:
  - "Text-Aware F1 - Hybrid F1" to show the effect of text density
  - Highlight layout-sensitive classes: form, invoice, memo, scientific_report
- Triple confusion matrix subplot (1x3) like hybrid notebook
- Analysis markdown cell discussing which classes improved/degraded

Use cell number comments consistently (e.g., `# C1: Setup`, `# C2: Load cache`, etc.) matching the pattern from the hybrid notebook.
  </action>
  <verify>
  Run: `poetry run python -c "
import json
import nbformat

# Verify baseline notebook has text density cell
nb = nbformat.read('notebooks/rvl-cdip-resnet50-baseline.ipynb', as_version=4)
cells_text = [c.source for c in nb.cells]
has_text_density = any('augment_cache_with_text_density' in c for c in cells_text)
has_section_71 = any('7.1' in c and 'Text Density' in c for c in cells_text)
print(f'Baseline notebook: text_density cell={has_text_density}, section 7.1={has_section_71}')
assert has_text_density, 'Missing augment_cache_with_text_density in baseline notebook'
assert has_section_71, 'Missing Section 7.1 header in baseline notebook'

# Verify text-aware notebook exists and has correct structure
nb2 = nbformat.read('notebooks/rvl-cdip-text-aware-hybrid.ipynb', as_version=4)
cells2 = [c.source for c in nb2.cells]
code_cells = [c for c in nb2.cells if c.cell_type == 'code']
md_cells = [c for c in nb2.cells if c.cell_type == 'markdown']
print(f'Text-aware notebook: {len(nb2.cells)} cells ({len(code_cells)} code, {len(md_cells)} markdown)')

# Check key imports
has_text_aware_model = any('TextAwareGraphSAGE' in c for c in cells2)
has_text_aware_graph = any('build_graph_dataset_text_aware' in c for c in cells2)
has_comparison = any('comparison' in c.lower() or 'Comparison' in c for c in cells2)
has_node_2051 = any('2051' in c for c in cells2)
# Check upstream metrics prerequisite assertions
has_baseline_metrics_check = any('resnet50_baseline_metrics.json' in c and 'assert' in c for c in cells2)
has_hybrid_metrics_check = any('hybrid_graphsage_metrics.json' in c and 'assert' in c for c in cells2)
print(f'TextAwareGraphSAGE={has_text_aware_model}, text_aware_graph={has_text_aware_graph}, comparison={has_comparison}, node_2051={has_node_2051}')
print(f'baseline_metrics_check={has_baseline_metrics_check}, hybrid_metrics_check={has_hybrid_metrics_check}')
assert has_text_aware_model, 'Missing TextAwareGraphSAGE import'
assert has_text_aware_graph, 'Missing build_graph_dataset_text_aware import'
assert has_comparison, 'Missing comparison section'
assert has_baseline_metrics_check, 'Missing assertion for resnet50_baseline_metrics.json'
assert has_hybrid_metrics_check, 'Missing assertion for hybrid_graphsage_metrics.json'
print('PASS')
"`
  </verify>
  <done>
  - Baseline notebook has new Section 7.1 with text density cache augmentation cell
  - The cell is additive (does not modify existing cells)
  - The cell uses samples= keyword matching Plan 01 definitive signature
  - The cell assumes data variable exists from prior cells
  - The cell does NOT re-run feature extraction or training
  - notebooks/rvl-cdip-text-aware-hybrid.ipynb exists with ~25 cells
  - Text-aware notebook imports TextAwareGraphSAGE and build_graph_dataset_text_aware
  - Text-aware notebook has 7 sections matching hybrid notebook structure
  - Section 7 has prerequisite assertions checking for resnet50_baseline_metrics.json and hybrid_graphsage_metrics.json
  - Comparison section loads all three model metrics for side-by-side analysis
  - Per-class F1 breakdown highlights layout-sensitive classes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Complete text-aware hybrid pipeline:
  1. Baseline notebook Section 7.1 — text density cache augmentation cell
  2. Text-aware hybrid notebook — full training and comparison pipeline

  Both build on Plan 01's text density extraction module and text-aware graph/model classes.
  </what-built>
  <how-to-verify>
  **Pre-check: Confirm upstream metric files exist**
  Before running the text-aware notebook's Section 7, verify these files from prior phases are present:
  - `checkpoints/resnet50_baseline_metrics.json` (from baseline notebook Section 8)
  - `checkpoints/hybrid_graphsage_metrics.json` (from Phase 2.1 hybrid notebook Section 6)
  If either is missing, run the corresponding prior notebook first.

  1. Open `notebooks/rvl-cdip-resnet50-baseline.ipynb` in Jupyter
     - Confirm Section 7.1 appears between Section 7 and Section 8
     - Confirm the cell references `augment_cache_with_text_density` with `samples=` keyword
     - Run the baseline notebook through Section 7.1 (requires cells C1-C4 + Section 7 first)
     - Verify cached .pt files now have `text_density` key

  2. Open `notebooks/rvl-cdip-text-aware-hybrid.ipynb` in Jupyter
     - Run all cells top to bottom
     - Verify training completes with early stopping
     - Verify evaluation produces per-class metrics
     - Verify comparison section loads all three model metrics
     - Check that per-class F1 table highlights form, invoice, memo, scientific_report

  3. Verify the comparison makes sense:
     - Text-aware hybrid should show different (ideally better) F1 on layout-sensitive classes
     - If results are similar, that's still valid — the experiment's value is in the comparison
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
End-to-end pipeline verification:
1. Baseline notebook Section 7.1 augments cache with text_density
2. Text-aware notebook loads augmented cache and verifies text_density key
3. Graph construction produces [49, 2051] node features
4. TextAwareGraphSAGE trains and evaluates
5. Section 7 prerequisite check verifies upstream metrics files exist before comparison
6. Comparison section shows three-way results
</verification>

<success_criteria>
- Baseline notebook has additive Section 7.1 cell (no existing cells modified)
- augment_cache_with_text_density called with samples= keyword matching Plan 01 signature
- Text-aware hybrid notebook runs end-to-end from cache loading through evaluation
- Section 7 has prerequisite assertions for checkpoints/resnet50_baseline_metrics.json and checkpoints/hybrid_graphsage_metrics.json
- Three-way comparison (CNN baseline vs hybrid vs text-aware hybrid) with per-class F1
- Layout-sensitive classes (form, invoice, memo, scientific_report) highlighted in comparison
- Metrics saved to checkpoints/text_aware_hybrid_metrics.json
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-text-aware-hybrid-gnn/02.2-02-SUMMARY.md`
</output>
